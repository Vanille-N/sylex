# Autogenerated by SyLeX

TWICE = true

DOC =

BUILDER = /absolute/path/to/sylex.py

all: $(DOC:%=%.pdf)

build/doc_%.tex.mk: cfg_%.slx $(BUILDER)
    $(BUILDER) name=$* fail=WARNING

build/common-tex.mk: $(BUILDER)
    $(BUILDER) common

texwatch: $(BUILDER)
    $(BUILDER) watcher

-include $(DOC:%=build/doc_%.tex.mk)
-include build/common-tex.mk

clean:
    rm -rf build
    rm $(DOC:%=%.pdf)

