{{header}}

{% for src,dest in copy %}
{{dest}}: {{src}}
    $(BUILDER) expand \
        --i $< \
        --o $@ \
        --features $(FEATURES)
{% endfor %}

{% for pre,post in extra %}
{{pre}}: {{post}} sylex.conf
{% endfor %}
